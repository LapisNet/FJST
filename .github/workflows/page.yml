name: Deploy Page

on: workflow_dispatch

jobs:
  Pull-and-Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Pull
        run: git clone https://github.com/Love-Kogasa/FJST.git
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2